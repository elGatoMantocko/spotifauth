/* eslint no-unused-vars: 1 */
const {login, refresh} = require('./index');
const sinon = require('sinon');
const chai = require('chai');

describe('test login function', function() {
});

describe('refresh login function', function() {
});
